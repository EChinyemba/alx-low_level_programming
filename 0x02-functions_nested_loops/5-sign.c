#include "main.h"

/**
 * main - shows sign of a number
 * Return: nothing
 */
int print_sign(int n)
{
int x = 0;

if(n > 0)
_putchar('+');

	return 1;

 if(n == 0)
 {
 _putchar(x + '0');

	return 0;
 }
else
_putchar('-');

	return -1;
}	
	
